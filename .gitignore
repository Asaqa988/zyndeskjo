/node_modules
/.next
/out
/build
.DS_Store
*.pem
npm-debug.log*
.env
.env*.local
.vercel
next-env.d.ts
*.tsbuildinfo

# Raw headshot used to derive public/avatar frames — not served, not shipped.
avatar-source.jpg

# Original un-cropped testimonial screenshots — names and photos visible.
# Never publish these; public/testimonials holds the cropped set.
.private/
.sheets/

# Scratch files from local experiments.
scripts/_*
